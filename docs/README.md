# Docs

- `MUSTSEE.md`: current frontend and gateway integration contract.
- `changeLog/`: per-model notes describing local modifications relative to upstream projects.

This folder is for project documentation that should stay close to the codebase but outside the main product entrypoints.
