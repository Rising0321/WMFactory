# Research References

This folder is for local study material and upstream reference repositories that inform WMFactory design work.

- `reference/` contains local reference repos such as `nano-vllm-omni` and `vllm-omni`.

These paths are intentionally separated from the main product surface so the root stays focused on runnable WMFactory components.
